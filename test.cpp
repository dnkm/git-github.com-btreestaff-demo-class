#include <bits/stdc++.h>
using namespace std;

void rec(vector<int> vv, int i) {
  if (i == 0)
    return;

  // vv.push_back(i);
  // rec(vv, i - 1);
  // cout << i << " ";
  // for (int v : vv)
  //   cout << v << " ";
  // cout << endl;
}

int main() {
  vector<int> vv;
  // vv.push_back(1);
  // rec(vv, 3);

  return -1;
}